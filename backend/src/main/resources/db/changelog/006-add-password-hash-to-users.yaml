databaseChangeLog:
  - changeSet:
      id: 006-add-password-hash-to-users
      author: dev
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: password_hash
                  type: varchar(255)
                  constraints:
                    nullable: true
