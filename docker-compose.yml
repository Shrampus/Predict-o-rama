services:
  frontend:
    image: ghcr.io/shrampus/predictorama-frontend:latest
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    image: ghcr.io/shrampus/predictorama-backend:latest
    ports:
      - "8080:8080"
    environment:
      - ALLOWED_ORIGIN=http://193.40.157.126
    restart: unless-stopped
